# Cursor Integration

## 安装方式

```bash
demandspec install cursor --project .
```

安装内容：

```text
.cursor/rules/demandspec.mdc
.demandspec/
demands/
```

Cursor 更适合通过 rules + 用户 prompt 的方式运行 DemandSpec。可以输入：

```text
按 DemandSpec /demandspec-prd 命令处理当前需求包。
```
